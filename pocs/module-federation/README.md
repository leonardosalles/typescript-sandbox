# Running the app

```
pnpm install
pnpm dev
```

then access http://localhost:3000 for host app and http://localhost:3001 for checkout app

# Issues

- Tailwind classes not loading if only present in remote app
- Storage and state are not real shared between host and remote app
